/**
 * CKEditor 5 build with FontFamily/FontSize (from backend/h5p/editor/ckeditor build).
 * Vite aliases `@ckeditor/ckeditor5-build-classic` here so app code can keep that import path.
 */
import ClassicEditor from '../vendor/ckeditor-text-block.js';

export default ClassicEditor;
